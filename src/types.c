#include "include/types.h"

ivec2 newIVec2(int x, int y) {
    ivec2 v;
    v.x = x;
    v.y = y;
    return v;
}

fvec2 newFVec2(float x, float y) {
    fvec2 v;
    v.x = x;
    v.y = y;
    return v;
}
